import 'package:flutter/material.dart';
import 'package:one_plus/generated/l10n.dart';

class LoginView extends StatelessWidget {
 LoginView({super.key,});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DFJDDDDDDDDDDD"),
      ),
    );
  }
}
