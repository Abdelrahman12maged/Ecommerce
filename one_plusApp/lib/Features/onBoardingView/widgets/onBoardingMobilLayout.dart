import 'package:flutter/material.dart';

class onBoardingMobileLayout extends StatelessWidget {
  const onBoardingMobileLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text(" mobile now"),);
  }
}